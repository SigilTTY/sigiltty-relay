node_modules/
dist/
.wrangler/
target/
.DS_Store

# Credentials and device data — never publishable. The Node entry defaults
# to DB_PATH=./sigiltty-relay.sqlite3 (src/node.ts), so running it from a
# checkout drops a database full of real APNs device tokens into the tree.
*.p8
.dev.vars*
*.sqlite3
*.sqlite3-*
